import { Canvas } from "@react-three/fiber";
import React from "react";
("use client");

type Props = {};

export default function ViewCanvas({}: Props) {
  return 
  <Canvas>
    <mesh>
        <boxGeometry />
    </mesh>

  </Canvas>;
}
